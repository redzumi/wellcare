type Question = [string, number, number]

export type PredictData = Array<Question>

const data:PredictData = [
    ['курение', 0.05, 1],
    ['гигиена', 0.1, 0.5],
    ['италия', 0.3, 0]
]

export default data;