@import '~src/variables.styl'

.header
  display flex
  padding 0 40px

  @media mobileMedia
    padding 0 16px

.row
  width 100%

.breadcrumb
  display flex
  justify-content center
  align-items center
  height 100%